test("dfs preview is working", () => {});

describe("binary tree operations should work", () => {
  test("insert should work", () => {});
  test("delete should work", () => {});
  test("update should work", () => {});
  test("search should work", () => {});
});
